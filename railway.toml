# Railway.toml - Railway deployment configuration
# https://docs.railway.app/deploy/config-as-code

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Run migrations before starting Gunicorn
startCommand = "python manage.py migrate --noinput && echo 'Starting Gunicorn...' && gunicorn pathaibackend.wsgi:application --bind 0.0.0.0:8000 --workers 2 --threads 4 --timeout 120 --log-level debug --capture-output --enable-stdio-inheritance"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Health check configuration
[deploy.healthcheck]
path = "/api/health/"
timeout = 100
interval = 30

# Resource configuration (optional, Railway auto-scales)
# [deploy.resources]
# replicas = 1

# Environment variable validation (Railway will prompt for these)
# Configure these in Railway dashboard
